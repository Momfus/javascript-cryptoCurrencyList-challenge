{
  "name": "javascript-cryptocurrencylist-challenge-vite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "copy": "cpx './src/**' dist/src",
    "dev": "npm run copy && vite",
    "build": "npm run build:copy && vite build",
    "build:copy": "cpx './src/**' dist/src",
    "preview": "vite preview"
  },
  "devDependencies": {
    "cpx": "^1.5.0",
    "vite": "^4.0.0"
  }
}
